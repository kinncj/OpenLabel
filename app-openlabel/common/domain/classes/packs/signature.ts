import type { ClassDef } from "@/common/domain/classes/types";

// Signature detection — single class
export const signatureClasses: ClassDef[] = [
  { id: 0, name: "signature", color: "#911eb4", source: "preset" },
];
